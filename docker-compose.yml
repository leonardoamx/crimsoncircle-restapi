services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: gbd#h3fg5
      MYSQL_DATABASE: crimsoncircle
      MYSQL_USER: crimsoncircle
      MYSQL_PASSWORD: an#h3d5ff3
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
